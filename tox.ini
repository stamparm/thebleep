[tox]
envlist = py{39,310,311,312,313,314}

[testenv]
deps = -rrequirements.txt
commands = pytest -v --capture=sys

[flake8]
ignore = E501,W503,W504
exclude = venv,build,.tox,setup.py,fastentrypoints.py,bench/.venvs
